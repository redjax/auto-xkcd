"""Packages join modules together into pre-defined pieces of code.

For example, the `xkcd_comic` package imports the `modules.xkcd_mod` package, then creates functions built around the code in that module.
"""
